
               <ul class="list-group">
               <div class="row">
               <div class="col-md-12" align="center">

                  <% if !(@doctor.followed_by?(current_doctor)) %>

                <%= link_to "Network", follow_doctor_path(@doctor), method: :post , :class => "btn btn-default glyphicon glyphicon-ok-sign" , :remote => true, :id=> "mylink" %> 

                <%end%>

                  <% if !(@doctor.liked_by?(current_doctor)) %>
                                 
                 <%= link_to "Öner", like_doctor_path(@doctor), method: :post , :class => "btn btn-success glyphicon glyphicon-star" , :remote => true, :id=> "mylink2" %> 

                 <%end%>

               </div>
             </div>
 
              </ul>