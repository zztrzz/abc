
      <!-- eğer doktor soruyu cevapladıysa edit edebilmeli cevaplamadıysa formla cevaplamalı -->


        <div class="input-group input-group-lg">
        <div class="panel panel-default">
        <div class="panel-heading"><div class="panel-title"><h4 class="glyphicon glyphicon-transfer">Cevapla</h4></div></div>
        <div class="panel-body">
     
            <%= form_for @pin.answers.new do |f| %>
       
             
             <%= f.hidden_field :pin_id, :value => @pin.id %>
             <%= f.hidden_field :doctor_id, :value => current_doctor.id %>

             <p><%= f.label " " %><br>
             <%= f.text_area :description, class: "form-control", rows: "4", maxlength: "200"  %>
             <span class="help-block">En fazla 200 karakter girebilirsiniz</span> 
            </p>
             <p>
             <%= f.label "Resim Yükle" %> 
             
              <div class="checkcontainer">
             <%= f.collection_radio_buttons(:photo_id, Photo.all, :id, :image_show)   %></div>   
 
             <div class="panel-footer">
                                   <%= f.fields_for :topics,f.object.topics.new do |x| %>
                                   <%= x.label :etiket %>
                                   <%= x.text_field :name%>
                                   <% x.hidden_field :answer_id, :value => f.object.id %>
                                   <%end%><br>

                                    <%= f.fields_for :topics,f.object.topics.new do |x| %>
                                     <%= x.label :etiket %>
                                     <%= x.text_field :name%>
                                     <% x.hidden_field :answer_id, :value => f.object.id %>
                                     <%end%><br>

                                    <%= f.fields_for :topics,f.object.topics.new do |x| %>
                                     <%= x.label :etiket %>
                                     <%= x.text_field :name%>
                                     <% x.hidden_field :answer_id, :value => f.object.id %>
                                     <%end%><br>

                                     <%= f.fields_for :topics,f.object.topics.new do |x| %>
                                     <%= x.label :etiket %>
                                     <%= x.text_field :name%>
                                     <% x.hidden_field :answer_id, :value => f.object.id %>
                                     <%end%><br>
 

               </div>
 
              <div class="actions">
                  <br>
                  <%= f.submit "Gönder", class: 'btn btn-success' %>
              </div>

      
      <%end%>
    </div></div>
      
     </div>
 